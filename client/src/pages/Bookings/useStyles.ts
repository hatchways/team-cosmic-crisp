import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {},
  bookings: {},
  paper: {
    padding: '2rem 1rem',
    margin: '1rem 0',
    border: 'none',
  },
  nextBooking: {},
  pastBookings: {
    marginTop: '1rem',
  },
}));

export default useStyles;
