import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  mainContainer: {
    minHeight: '90vh',
    background: '#f5f5f5',
  },
  mainGrid: {
    width: '100%',
    padding: '3.5rem 7rem',
  },
}));

export default useStyles;
