import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    margin: '2rem auto 0',
  },
  paper: {
    padding: '2rem 3rem',
    minWidth: '100%',
  },
}));

export default useStyles;
