import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  profilesGrid: {
    minHeight: '50vh',
  },
  paginationContainer: {
    marginTop: '3.5rem',
  },
}));

export default useStyles;
