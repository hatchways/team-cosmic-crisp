import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  sortContainer: {
    margin: '1.5rem 0rem',
  },
  sortBtn: {
    fontSize: '1rem',
    margin: '0rem 0.75rem',
  },
  profilesGrid: {
    minHeight: '50vh',
  },
  paginationContainer: {
    marginTop: '3.5rem',
  },
}));

export default useStyles;
